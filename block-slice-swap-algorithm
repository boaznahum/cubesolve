in this session we will work on block swap by slice swip.

most of therotical and concepts are taken from exist commutator helper "ch" and Face2FaceTransltirvamost new capabilities are needed
all the new code will be imlement basicly in new class under geometry package were ch is and it will have similiar methods
the class will called BlockBySliceSwapHelper

other claases cide to look and search for answers : block and c9nceot of kernel and ordering, cybe layout and suzef cube layout 


some theory 

we have a block on face, the block coordinates are r1,c1,r2,c2 see class block, we have no way to represent zero suze block other then none

our task is to swap the target block with source block from source target, so we have the terms source , target face and block
how we do the swap, we do a slice operartion e,m, s bringing slices from source face near the target block, rotating the target face
90 or 180 - each have diffrent concerns that we need to discuss - and bringing doing the prime slice opearuin, so the block on the
target moved to the visiting source slices swap with block on the source, now when the slices go back to source face by the inverse 
slice opeartion we swap blocks.
this is simple algorithm much simpler the commutator but the problem is that it swaps all the slices not only the block

lets take an example , there are few paramers, 
n number of skices in cube
ifvwe rotate 90 or 180
does thevskice is vertical or horizontal on source face , please not that horizontal slice on source can be vertical on target

in this example we say the slice is vertical on source
we rotate 180
number of slices is nn
block on target is r1,c1,r2,c2

if we rotate the target block by 180 then it has new coordinstes r1',c1',r2',c2' while x'=inv(x) , see defenition of inv in geometry packake , basicly it is nn-1-1
in case of 90 we have similar rules are come from the rules of rotating blocks

first in order to perfrom the swap the block must mot interact with it self , becase it is vertical slice then r1,r2 must not overlap with r1',r2'
in case of hirizontal slice this rules aplly on the column coordinates, c1,c2,c1',c2'

so lets say the condition met, then we can the swap, we bring the souce slices, what are the source slices ? they are r1' to r2' in face coordinates that need to be converted to slices indexies
so we bring the source slices, rotate, now target block is on the source slices and the source block is on the target skices
now we send back the siurce slices to their source face taking with them the target block


'
