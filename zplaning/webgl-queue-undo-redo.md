i want do developer a feature of queue and user assit

this how it work,

first i want to move the redo undo operation tohe the server Operator Prorocol

when ever we want to do undo operation it is done by the operaor like any other play, but it aslo enter the undo queue
when ever we do a redo it is normal play operation the enter the list of las t operation from where we can see the hostory and doing undo

so task number one: undo redo queue
1. in the left side of the screen i wan to see an elegant queu of operation where we can see the last N operation and the operation we can redo becuase we undo
talk to your agents for an elegant UI desgin, a profession i dont want to see a simple one.
2. then two beatiful button for undo redo, an undo take from the last, play it and it enter to the redo, all of this is done by current OperatorProtocol
3. wehne thre is an redo operation (we will late ruse this feature to teach developer to solve cube) there be a marker on the cube, a beatiful signs on
  on the cells (not above like the arrrows we ddi when developer touch the screen) but beatiful assist signe thta seems like part of the cell itself
4. if there no redo then of course it will disparear
5. also a beatiful button for fast replay, it will he same mecahnsim like the solver today that play sequnece and the stop bottom logic is according to it
6. actaully i wantt o desgin the solver operation like a private acse of redo so lets explain it now
7. When user press solve, instead of askiug t solution, you ask Operator to queu the solution so now the
8. solutin is in the que, but is not played, pressing the fast play (or rewind i dont know) play it like todoy, if not the use simply press redo,
9. but we may consider chnage the name of the button in this case to 'next' or something like this, in way alwas the signs on the cube i aske dbefore are displayed
10. the is a question !! when solving what to to with the elements that were in the redo qeue, if we left them there than fast pkaying the solutiin
11.  will not play sole the cube.
12. please plan, make sure you understand, consult GUI expert agent, ask if something is not clear, say if you need to move to react or implemnt some state mecanism
13. i dont want you to invite the wheel, if there are such frame work use it, choose the best