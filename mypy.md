python -m pip install -e .[dev]

mypy -p cube

 Yes, pyproject.toml replaces mypy.ini. Let me consolidate them and make pyproject.toml the single source of truth:


 switch terminal to classic so output is cliackab;e



