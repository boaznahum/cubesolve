but when i rain_g wuth sequence 1/q there is no issue, maybe you run the test with debug=true il will do the same from main_g and we can compare ? you can add fixture to debug turn on

The the  run once, maybe is because we repelace deprecated methods ?
s